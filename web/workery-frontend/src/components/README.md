```
components/
├── UI/           (pure, reusable UI components)
├── business/     (domain-specific components)
│   ├── selects/  (all smart select components)
│   ├── displays/ (all smart display components)
│   └── shared/   (shared business logic/hooks)
└── common/       (shared non-UI, non-business components)
```
