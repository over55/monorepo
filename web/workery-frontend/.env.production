# FILE: .env.production

# API Configuration
VITE_API_DOMAIN=theworkery.cloud
VITE_API_PROTOCOL=https

# Frontend Configuration (Note: (v1)=workeryapp | (v2)=workeryweb)
VITE_WWW_DOMAIN=workeryweb.ca
VITE_WWW_PROTOCOL=https

# Upload Configuration (if needed)
VITE_IMAGE_UPLOAD_MAX_FILESIZE_IN_BYTES=5242880
VITE_IMAGE_UPLOAD_MAX_FILESIZE_ERROR_MESSAGE=File size must be less than 5MB

# Password Storage Configuration
# ALWAYS use sessionStorage in production for security
VITE_PASSWORD_STORAGE_MODE=sessionStorage

# Development mode flag
VITE_DEV_MODE=false
